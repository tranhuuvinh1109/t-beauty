export type CustomerType = {
  id: string | number;
  name: string;
  phone: string;
  email: string;
  address: string;
  image?: string;
  dateOfBirth: string;
  createdAt?: string;
  updatedAt?: string;
};
